DATABASE_URL=postgresql+psycopg://traction:traction@localhost:5432/traction

# Generate a real value before deploying, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-only-change-me

